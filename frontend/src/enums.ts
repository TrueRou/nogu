export enum Gamemode {
    VN_OSU = 0,
    VN_TAIKO = 1,
    VN_CATCH = 2,
    VN_MANIA = 3,
    RX_OSU = 4,
    RX_TAIKO = 5,
    RX_CATCH = 6,
    AP_OSU = 8,
}

export enum Privacy {
    PUBLIC = 0,
    PROTECTED = 1,
    PRIVATE = 2,
    BOCCHI = 3,
}

export enum MemberPosition {
    EMPTY = 0,
    MEMBER = 1,
    MANAGER = 2,
    OWNER = 3,
}

export enum Server {
    LOCAL = 0,
    BANCHO = 1,
}
